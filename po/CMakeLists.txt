# Translation file configuration

find_package(Gettext)

if (GETTEXT_FOUND)
    # Placeholder for translation support
    # This can be enhanced to use weblate like dnf-plugins-extras
    message(STATUS "Gettext found - translation support enabled")
endif()
